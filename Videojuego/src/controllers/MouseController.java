package controllers;

public class MouseController {

/*	private float px, py; //posicion de nuestro cuerpo en pantalla
	private float ox, oy; //posicion de origen del cuerpo
	private float dx, dy; //posicion destino del cuerpo
	private float vx, vy; //velocidad de movimiento del cuerpo
	private Semaforo estado;
	private int dc; //delta contador
	private static final float T=0.5f;

	public void mouseReleased(int button,int x,int y) {
		if(button !=0) return;
		
		dx= (float)x;
		dy= (float)y;
		ox= px;
		oy=py;
		
		vx = (dx - ox)/T;
		vy = (dy - oy)/T;
		
		dc=0;
		estado= Semaforo.MOVIMIENTO;
		
	}
	
	
	
}

enum Semaforo{
	DETENIDO,
	MOVIMIENTO;
}*/
}